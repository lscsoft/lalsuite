target-version = "py310"
extend-exclude = [
    "*.ipynb",
]

[lint]
select = [
    # isort errors
    "I",
    # pycodestyle errors
    "E9",
    # executable issues
    "EXE",
    # pyflakes
    "F401",
    # pycodestyle warnings
    "W2",
    "W605",
]
