repos:

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-yaml
        files: ^\.pre-commit-config.yaml$

      - id: check-yaml
        args: [--unsafe]
        files: ^\.gitlab.*\.yml$
